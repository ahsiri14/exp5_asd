pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the application from Git...'
            }
        }

        stage('Test') {
            steps {
                echo 'Running automated tests...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application.'
            }
        }
    }
}
